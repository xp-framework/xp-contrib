package org.company.app.hello.unittest;

import unittest.TestCase;
import org.company.app.hello.Hello;

/**
 * Tests for org.company.app.hello.Hello
 *
 */
public class HelloTest extends TestCase {

  /**
   * Test constructor
   *
   */
  [@test] public void should_be_able_to_create_Hello_instances() {
    $this.assertClass(new Hello(), 'org.company.app.hello.Hello');
  }
}
